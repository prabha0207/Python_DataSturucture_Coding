class node:
    def _init_(self,data):
        self.data=data
        self.next=None
class slist:
    def _init_(self):
        self.head=None
def ins_at_beg(data):
    if(head==None):
        head=tail=None
    else:
        newnode=node(data)
        newnode.next=self.head
        self.head=newmode
        newnode=None
def ins_at_end(self,data):
    newnode=node(data)
    self.tail.next=newnode
    self.tail=newnode
    newnode=None
def ins_at_anypos(self,data,.pos):
    newnode=node(data)
    temp=self.head
    for i in range(1,pos-1):
        temp=temp.next
        newnode.next=temp.next
        temp.next=newnode
        temp=newnode=None


def display(self)
temp=self.head
while(temp is not None):
    print(temp.data,end="")
    temp=temp.next

    s=slist()
    s.ins_at_beg(10)
    s.ins_at_beg(20)
    s.ins_at_beg(30)
    s.ins_at_end(89)
    s.ins_at_end(90)
    s.ins_at_anypos(2.33)
    s.ins_at_anypos(3,44)
    s.display()
    
    
    
    
        
    
